# Shared UI Components
No shared UI primitives in the Python backend. The React app inside /frontend has UI primitives in App.jsx, but the target Flask application will utilize a unified interactive template system.
